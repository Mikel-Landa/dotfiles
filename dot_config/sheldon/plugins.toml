# Sheldon configuration file
#
# See https://sheldon.cli.rs

shell = "zsh"

[templates]
defer = '''
    {{- hooks?.pre | nl }}
    {%- for file in files %}zsh-defer source "{{ file }}"{{ "\n" }}
    {%- endfor %}
    {{- hooks?.post | nl }}'''

# Completions
# -----------

# [plugins.zsh-autocomplete]
# github = "marlonrichert/zsh-autocomplete"

[plugins.docker-zsh-completion]
github = "greymd/docker-zsh-completion"

[plugins.zsh-completions]
github = "zsh-users/zsh-completions"

# Sourced
# -------

[plugins.local-0]
local = "~/.config/zsh/plugins"
use = ["history.plugin.zsh"]

[plugins.powerlevel10k]
github = "romkatv/powerlevel10k"
use = ["powerlevel10k.zsh-theme"]

[plugins.zsh-defer]
github = "romkatv/zsh-defer"

[plugins.autoload]
local = "~/.config/zsh/plugins"

# Deferred plugins
# ----------------

[plugins.local-1]
local = "~/.config/zsh/plugins"
use = ["{aliases,gpg-agent,keybindings}.plugin.zsh"]
apply = ["defer"]

[plugins.zsh-you-should-use]
github = "MichaelAquilina/zsh-you-should-use"
apply = ["defer"]
hooks.pre = "export YSU_MODE=ALL"

[plugins.color-man-pages]
github = "ohmyzsh/ohmyzsh"
use = ["plugins/colored-man-pages/colored-man-pages.plugin.zsh"]
apply = ["defer"]
hooks.pre = "autoload -U colors && colors"

[plugins.fast-syntax-highlighting]
github = "zdharma-continuum/fast-syntax-highlighting"
apply = ["defer"]

[plugins.forgit]
github = "wfxr/forgit"
rev = "26.01.0"
use = ["forgit.plugin.zsh"]
apply = ["defer"]
hooks.pre = "export forgit_checkout_commit=fgco"

# [plugins.zsh-vi-mode]
# github = "jeffreytse/zsh-vi-mode"
# use = ["{{name}}.zsh"]
# apply = ["defer"]

# [plugins.zsh-kubectl-prompt]
# github = "superbrothers/zsh-kubectl-prompt"
# use = ["kubectl.zsh"]
# apply = ["defer"]

# This works best if it is placed last.
[plugins.compinit]
local = "~/.config/zsh/plugins"
apply = ["defer"]

# After compinit so completions work
[plugins.evals]
local = "~/.config/zsh/plugins"
apply = ["defer"]

# Must load LAST: after syntax-highlighting, compinit, and any plugin/keybinding
# that creates or rebinds ZLE widgets. Otherwise widget wrap order breaks
# partial-accept (ctrl-right) and clear-on-tab, causing ghost-text doubling.
[plugins.zsh-autosuggestions]
github = "zsh-users/zsh-autosuggestions"
use = ["{{ name }}.zsh"]
apply = ["defer"]
hooks.pre = "export ZSH_AUTOSUGGEST_MANUAL_REBIND=1"
