#!/usr/bin/env bash
set -euo pipefail

rm -rf .out
mkdir -p .out
crossplane render --observed-resources observed --include-full-xr examples/composite.yaml apis/composition.yaml examples/functions.yaml | tee .out/full.yaml | kubectl-slice --template '{{.kind | lower}}-{{.metadata.name}}.yaml' --output-dir .out

